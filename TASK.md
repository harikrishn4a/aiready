# TASK.md — feat-019: Expanded canonical set, generateOnly flag, remove score-gated SKIP

## Feature ID
feat-019

## Scope — what will change
- UPDATED `src/audit/remediation.ts` — expand CANONICAL_ARTIFACTS to 19, add generateOnly flag, export CANONICAL_ARTIFACTS + SOURCE_ONLY_FILES + isEmpty, update buildRemediationPlan decision logic (no score-gated SKIP)
- UPDATED `src/init/parser.ts` — expand TEMPLATE_BY_FILENAME and BLANK_TEMPLATE_FILES to cover all 19
- UPDATED `src/init/planner.ts` — buildInitPlan() re-derives from CANONICAL_ARTIFACTS + file existence; no longer reads GENERATE/IMPROVE/SKIP from plan.md; add generateOnly to ArtifactPlan
- UPDATED `src/init/generator.ts` — expand BLANK_TEMPLATE_FILES to all 19 generateOnly filenames
- UPDATED `tests/remediation.test.ts` — 19-count tests, generateOnly tests, updated skip/improve tests
- UPDATED `tests/init-planner.test.ts` — rewrite for new file-existence-based decision logic

## Problems being fixed
1. Canonical artifact set is incomplete — 6 example templates not generated by init
2. SKIP logic gates improvement on score >= 80 — should always improve non-generateOnly artifacts
3. Makefile/scripts are structural — generate once, never auto-improve (generateOnly=true)

## Design decisions
- generateOnly=true → GENERATE if missing or empty, SKIP if exists with content
- generateOnly=false → GENERATE if missing, IMPROVE if exists (always, no score threshold)
- buildInitPlan() re-derives from CANONICAL_ARTIFACTS + filesystem (plan.md provides subsystemSources + overall + sourceContext only)
- isEmpty(): strip note lines (^>.*$) and placeholders ({{...}}), < 50 chars → empty
- template paths keep 'examples/' prefix for consistency with existing TEMPLATE_BY_FILENAME

## New artifacts (6)
- structure.md — memory, generateOnly=false
- feature-list-schema.json — null, generateOnly=true
- quality-document.md — null, generateOnly=true
- evaluator_rubric.md — null, generateOnly=true
- clean-state-checklist.md — null, generateOnly=true
- startup.md — verification, generateOnly=true

## generateOnly=true artifacts (9 total)
TASK.md, features.md, feature_list.json, feature-list-schema.json, QUALITY.md,
quality-document.md, evaluator_rubric.md, clean-state-checklist.md, startup.md,
Makefile, scripts/init.sh, scripts/verify.sh

## Completion gate
- npm run build — zero errors
- npm run typecheck — zero errors
- npm run lint — clean
- npm test — all pass
