# my-project

> **Example** — A bare-minimum repo with no harness files. Scores 0/100 on AI readiness audit.

A project that has not yet been harnessed for AI agents.

## Installation

```
npm install
npm start
```

This project lacks AGENTS.md, ARCHITECTURE.md, PROGRESS.md, CONSTRAINTS.md, and SESSION-HANDOFF.md.
Run `npx aiready init` to generate the missing harness artifacts.
